%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Beamer Presentation
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass{beamer}
\mode<presentation> {
  \usetheme{default}
  \usecolortheme{orchid}
  \setbeamertemplate{footline}[frame number]
  \setbeamertemplate{navigation symbols}{}
}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{bm}
\usepackage{array}
\usetikzlibrary{shapes.geometric, arrows, positioning}

\newcommand{\argmin}{\mathop{\mathrm{argmin}}\limits}
\newcommand{\argmax}{\mathop{\mathrm{argmax}}\limits}
\newcommand{\dens}{\mathrm{dens}}
\newcommand{\A}{A}
\newcommand{\F}{F}

\title[]{Densest Community Search}
\subtitle{Query-anchored dense subgraphs in directed citation networks}

\author{The-Anh Vu-Le}
\institute[UIUC]{
University of Illinois Urbana-Champaign \\
\medskip
\textit{vltanh@illinois.edu}
}
\date{}

% Insert a divider frame at the start of each of the four top-level sections
% (Motivation and Problem Statements, Approaches, Experiments, Conclusion) so
% the audience sees the structural break clearly in the deck.
\AtBeginSection[]{
  \begin{frame}
    \centering
    \usebeamerfont{title}\insertsectionhead\par
  \end{frame}
}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

\section{Motivation and Problem Statements}

\begin{frame}{Motivation}
  \begin{itemize}
    \item Citation networks: nodes are papers, directed edges are citations
    \item Query: a paper $q$
    \item Goal: retrieve a compact, coherent citation community around $q$
    \item Baseline: take immediate citation neighbors
    \item Issue: immediate neighbors are not an optimization problem
  \end{itemize}

  \vfill

  \textbf{Question} Can we search for a neighborhood that is dense, query-centered, and structurally constrained?
\end{frame}

\begin{frame}{What makes this hard?}
  \begin{itemize}
    \item The objective is fractional:
    \[
      \frac{|E(S)|}{|S|(|S|-1)}
    \]
    \item The decision variables are binary: select papers or not
    \item The graph can be huge, or only available through an oracle
    \item Connectivity constraints are global
    \item Exact search can be expensive even on moderate local graphs
  \end{itemize}

  \vfill

  \textbf{Design choice} Use exact optimization machinery, but expose time limits and bounds.
\end{frame}

\begin{frame}{Combinatorial problem}
  Given
  \begin{itemize}
    \item directed graph $G=(V,E)$
    \item query node $q \in V$
    \item minimum size $k \ge 3$
  \end{itemize}

  \vfill

  Solve
  \begin{align*}
    \max_{S \subseteq V} \quad &
      \frac{|E(S)|}{|S|(|S|-1)} \\
    \mathrm{s.t.} \quad &
      q \in S \\
    & |S| \ge k
  \end{align*}

  \vfill

  where $E(S)=\{(u,v)\in E \mid u,v\in S\}$.
\end{frame}

\begin{frame}{Optional connectivity}
  \textbf{Rooted $\kappa$-edge-connectivity}

  \vfill

  For every selected node $v \in S \setminus \{q\}$:
  \[
    \text{there are at least } \kappa
    \text{ edge-disjoint paths from } q \text{ to } v.
  \]

  \vfill

  \begin{itemize}
    \item $\kappa = 0$ disables the constraint
  \end{itemize}
\end{frame}

\begin{frame}{Nonlinear fractional 0-1 program}
  Let
  \begin{itemize}
    \item $x_v \in \{0,1\}$ indicate $v \in S$
    \item $y_{uv} \in \{0,1\}$ indicate directed edge $(u,v)$ is internal
  \end{itemize}

  \begin{align*}
    \max \quad &
      \frac{\sum_{(u,v)\in E} y_{uv}}
      {\left(\sum_{u\in V}x_u\right)
       \left(\sum_{u\in V}x_u - 1\right)}
       \\
    \mathrm{s.t.} \quad &
      x_q = 1 \\
    & \sum_{u\in V} x_u \ge k \\
    & y_{uv} \le x_u,\quad y_{uv}\le x_v
      \quad \forall (u,v)\in E
  \end{align*}

  \vfill

  \textbf{Note} The lower bound $y_{uv}\ge x_u+x_v-1$ is not needed because $y$ has positive objective coefficient.
\end{frame}

\section{Approaches}

\begin{frame}{Dinkelbach transform}
  For a density estimate $\lambda$, define
  \[
    Z(\lambda)
    =
    \max_S
    \left\{
      |E(S)| - \lambda |S|(|S|-1)
    \right\}.
  \]

  \vfill

  \textbf{Key property}
  \[
    \lambda^\star \text{ is optimal}
    \quad \Longleftrightarrow \quad
    Z(\lambda^\star)=0.
  \]

  \vfill

  \begin{itemize}
    \item If $Z(\lambda)>0$, an improving denser set exists
    \item Update $\lambda \gets |E(S)|/[|S|(|S|-1)]$
    \item Stop when the parametric subproblem cannot improve density
  \end{itemize}
\end{frame}

\begin{frame}{Dinkelbach loop}
  \begin{enumerate}
    \item Build an initial feasible set by BFS from $q$
    \item Prune it to improve density
    \item Set $\lambda$ to the current density
    \item Solve the parametric integer subproblem
    \item If objective $\le 0$, stop
    \item Otherwise update $\lambda$ and repeat
  \end{enumerate}

  \vfill

  \textbf{Outer loop} handles the fractional objective.

  \vfill

  \textbf{Inner loop} solves the parametric problem by branch-and-price.
\end{frame}

\begin{frame}{Linearizing the penalty}
  The Dinkelbach penalty is
  \[
    |S|(|S|-1)
    =
    \left(\sum_v x_v\right)^2 - \sum_v x_v.
  \]

  Since $x_v^2=x_v$ for binary variables:
  \[
    |S|(|S|-1)
    =
    2\sum_{u<v}x_u x_v.
  \]

  \vfill

  Introduce pair variables:
  \[
    w_{uv} \approx x_u x_v,
    \qquad
    w_{uv} \ge x_u+x_v-1.
  \]

  \vfill

  Because $w$ has negative coefficient, this lower bound is enough at integer solutions.
\end{frame}

\begin{frame}{Oracle model}
  The solver does not need the whole graph.

  \vfill

  \begin{itemize}
    \item each oracle call exposes one paper's 1-hop citation
      neighborhood
    \item the solver pulls more papers only when pricing decides
      they help
  \end{itemize}

  \vfill

  Maintained sets:
  \begin{itemize}
    \item $A$: active nodes in the restricted master problem
    \item $F$: frontier nodes discovered but not active
    \item cached in/out adjacency lists
    \item pending edges whose endpoint variables are not both
      created yet
  \end{itemize}
\end{frame}

\begin{frame}{Directionality and the incoming-citation cap}
  Edge convention:
  \[
    u \to v
    \quad \Longleftrightarrow \quad
    \text{paper } u \text{ cites paper } v.
  \]

  \vfill

  Each oracle query for a paper $v$ returns:
  \begin{itemize}
    \item papers referenced by $v$ (outgoing)
    \item papers that cite $v$ (incoming)
  \end{itemize}

  \vfill

  \textbf{Incoming-citation cap}
  \begin{itemize}
    \item incoming-edge queries can be more expensive than outgoing:
    \begin{itemize}
      \item reference list is usually small, cited-by list is
        unlimited
      \item references read off the paper itself; cited-by needs a
        maintained database
    \end{itemize}
    \item the cap limits how many incoming edges are fetched per
      queried node
    \item reported runs fetch only outgoing citations, no incoming
  \end{itemize}
\end{frame}

\begin{frame}{Active set initialization}
  \begin{itemize}
    \item Start from query $q$
    \item BFS outward until $|A|\ge k$
    \item Query any active node that was added as a neighbor but not expanded
    \item Add unseen adjacent nodes to frontier $F$
    \item Blacklist nodes that fail oracle retrieval
  \end{itemize}

  \vfill

  \textbf{Purpose}
  \begin{itemize}
    \item guarantee a feasible initial model
    \item avoid loading the full citation graph
    \item seed Dinkelbach with a meaningful local density
  \end{itemize}
\end{frame}

\begin{frame}{Restricted Master Problem}
  For active set $A$:

  \begin{align*}
    \max \quad &
      \sum_{(u,v)\in E(A)} y_{uv}
      -2\lambda\sum_{\substack{u,v\in A\\u<v}}w_{uv} \\
    \mathrm{s.t.}\quad &
      x_q = 1 \\
    & \sum_{u\in A}x_u \ge k
      \quad (\text{dual: }\pi) \\
    & y_{uv}\le x_u,\quad y_{uv}\le x_v
      \quad \forall (u,v)\in E(A) \\
    & w_{uv}\ge x_u+x_v-1
      \quad \forall u<v \in A \\
    & 0\le x,y,w\le 1
  \end{align*}

  \vfill

  This LP gives the bound inside branch-and-bound.
\end{frame}

\begin{frame}{Pricing a frontier node}
  Let $\bar{x}$ be the RMP solution.

  \vfill

  Fractional degree of a frontier node:
  \[
    \mathrm{fracdeg}(f)
    =
    \sum_{v\in N(f)\cap A}\bar{x}_v.
  \]

  Reduced cost score:
  \[
    \bar{c}_f
    =
    \mathrm{fracdeg}(f)
    -2\lambda\sum_{v\in A}\bar{x}_v
    -\pi.
  \]

  \vfill

  \begin{itemize}
    \item edge gain: $\mathrm{fracdeg}(f)$
    \item size-pair penalty: $-2\lambda\sum_v\bar{x}_v$
    \item size dual adjustment: $-\pi$
  \end{itemize}
\end{frame}

\begin{frame}{Column generation}
  \textbf{Pricing rule}
  \[
    f^\star = \argmax_{f\in F}\bar{c}_f.
  \]

  \vfill

  \begin{itemize}
    \item If $\bar{c}_{f^\star}\le 0$, no frontier node improves the LP
    \item If $\bar{c}_{f^\star}>0$, promote frontier nodes into $A$
    \item The implementation adds a batch, not just one node
  \end{itemize}

  \vfill

  Batch size:
  \[
    b =
    \mathrm{clamp}
    \left(
      \lfloor \rho |A| \rfloor,\ b_{\min},\ b_{\max}
    \right).
  \]

  \vfill

  \textbf{Interpretation} Column generation is also graph expansion.
\end{frame}

\begin{frame}{Branch-and-bound}
  Each B\&B node stores:
  \begin{itemize}
    \item $V_1$: nodes fixed into the solution
    \item $V_0$: nodes fixed out of the solution
  \end{itemize}

  \vfill

  At each node:
  \begin{enumerate}
    \item apply bounds to the persistent RMP
    \item run column generation
    \item prune if LP bound cannot beat incumbent
    \item branch if $\bar{x}$ is fractional
    \item check integer solution if $\bar{x}$ is integral
  \end{enumerate}

  \vfill

  Root node fixes $q$ into the solution.
\end{frame}

\begin{frame}{Branching rule}
  A fractional node is judged by its fractional internal degree.

  \vfill

  \[
    d_{\mathrm{frac}}(v)
    =
    \sum_{u\in N(v)\cap A}\bar{x}_u.
  \]

  \vfill

  \begin{itemize}
    \item If $d_{\mathrm{frac}}(v) < 2\lambda$:
    \begin{itemize}
      \item $v$ is a hanging node
      \item branch zero-first
    \end{itemize}
    \item Otherwise:
    \begin{itemize}
      \item $v$ is more core-like
      \item branch one-first
    \end{itemize}
  \end{itemize}

  \vfill

  \textbf{Reason} $2\lambda$ is the marginal pair penalty threshold.
\end{frame}

\begin{frame}{Triangle cuts}
  LP relaxation can spread mass across nodes while keeping pair variables small.

  \vfill

  Add Boolean quadratic polytope cuts:
  \[
    x_u+x_v+x_w
    -w_{uv}-w_{vw}-w_{uw}
    \le 1.
  \]

  \vfill

  \begin{itemize}
    \item valid for binary $x$ and $w=x_ix_j$
    \item separated only on fractional nodes
    \item cap: 20 violated cuts per round
    \item skipped when LP gap is already small
  \end{itemize}

  \vfill

  \textbf{Goal} tighten the bound without overwhelming the RMP.
\end{frame}

\begin{frame}{$\kappa$-connectivity cuts}
  For an integer candidate:
  \begin{enumerate}
    \item Build unit-capacity support graph on selected edges
    \item For each $t\in S\setminus\{q\}$, compute max flow $q\to t$
    \item If flow $<\kappa$, extract min-cut $R$
    \item Add cut inequality
  \end{enumerate}

  \vfill

  \[
    \sum_{\{u,v\}\in \delta(R)} y_{uv}
    \ge
    \kappa x_t.
  \]

  \vfill

  \textbf{Lazy enforcement} Only add connectivity constraints when a candidate violates them.
\end{frame}

\begin{frame}{Pruning integer incumbents}
  Since the constraint is $|S|\ge k$, solutions may be larger than needed.

  \vfill

  Greedy pruning:
  \begin{itemize}
    \item never remove query node $q$
    \item compute internal degree of each selected node
    \item remove the least useful node if objective improves
    \item stop at size $k$ or when no removal helps
    \item preserve connectivity when $\kappa>0$
  \end{itemize}

  \vfill

  \textbf{Effect} stronger incumbent, smaller returned community.
\end{frame}

\begin{frame}{Average-degree baseline: pipeline}
  \textbf{Problem}
  \begin{itemize}
    \item maximise $|E(S)| / |S|$ over $S \ni q$,
      $S \subseteq V_{\mathrm{loc}}$
    \item $V_{\mathrm{loc}}$ is BFS-bounded
    \item no size constraint, no connectivity constraint, no triangle
      relaxation
    \item avg degree, not pair density
  \end{itemize}

  \vfill

  \textbf{Pipeline}
  \begin{itemize}
    \item BFS from $q$ caches the local subgraph once
    \item Dinkelbach bisects $\lambda \in [0, |E_{\mathrm{loc}}|]$
    \item per $\lambda$: Goldberg $s$--$t$ flow + push-relabel
    \item $S_\lambda$ = source-side residual
    \item argmax density across every probed $\lambda$
  \end{itemize}
\end{frame}

\begin{frame}{Average-degree baseline: four adaptations of Goldberg 1984}
  \begin{itemize}
    \item \emph{Query anchor.} $s \to q$ capacity $\infty$ keeps $q$ on
    the source side of every cut, so the answer is query-anchored
    densest.
    \item \emph{Local subgraph.} BFS bound replaces Goldberg's
    fully-materialised graph; required for the lazy oracle.
    \item \emph{Directed unit-weight edges.} Each directed edge
    $(u, v)$ contributes one edge-item, so density counts directed
    edges; a reciprocal pair $(u, v), (v, u)$ contributes two.
    \item \emph{Objective.} Avg degree, not pair density. The two
    objectives are not monotone in each other.
  \end{itemize}
\end{frame}

\section{Experiments}

\begin{frame}{Exp 1: solver correctness on enumerable graphs}
  \textbf{Goal}
  \begin{itemize}
    \item check that the solver recovers the true optimum on graphs
      small enough to enumerate every subset
  \end{itemize}

  \vfill

  \textbf{Fixture}
  \begin{itemize}
    \item Erd\H{o}s--R\'enyi random graphs $G(n, p)$
    \item $n = 20$
    \item $p \in \{0.25,\, 0.50,\, 0.75\}$
    \item seeds $0..4$, so 15 graphs total
    \item query $q$: highest-degree node per graph
  \end{itemize}
\end{frame}

\begin{frame}{Exp 1: brute-force oracle}
  For every $S \ni q$ with $|S| \ge k$:
  \begin{itemize}
    \item iterate $k \in \{3, 4, 5\}$, $\kappa \in \{0, 1, 2\}$
    \item record the maximum-density $S$ subject to
      $\lambda(S) \ge \kappa$
    \item mark cells with no feasible $S$ at the requested $\kappa$
      as infeasible
  \end{itemize}
\end{frame}

\begin{frame}{Exp 1: solver protocol}
  \begin{itemize}
    \item run the solver on every (method, $k$, $\kappa$, seed) cell
    \item no time, node, gap, or Dinkelbach limit
    \item skip cells the brute force already flagged infeasible:
    \begin{itemize}
      \item otherwise the solver exhausts the search proving
        infeasibility
      \item wall time blows up on sparse seeds
    \end{itemize}
  \end{itemize}
\end{frame}

\begin{frame}{Exp 1: result}
  \textbf{AvgDeg}
  \begin{itemize}
    \item matches the unconstrained avg-degree optimum on
      $15 / 15$ graphs at every $p$
  \end{itemize}

  \vfill

  \textbf{BP}
  \begin{itemize}
    \item matches the edge-density optimum on every feasible
      $(k, \kappa, p)$ cell
    \item two cells at $k = 3$, $\kappa \in \{1, 2\}$, $p = 0.5$
      contain one seed with $\lambda(S) < \kappa$
    \item that seed is surfaced as infeasible-$\kappa$, not as a
      non-match
  \end{itemize}
\end{frame}

\begin{frame}{Citation dataset and split}
  Dataset: CitationFull Cora-ML (lead dataset)
  \begin{itemize}
    \item 2,995 nodes
    \item 8,416 directed edges
    \item 7 paper labels
  \end{itemize}

  \vfill

  Query pool: papers that only cite (pure sources) with out-degree
  $\ge 2$.
  \begin{itemize}
    \item val: 402 queries
    \item test: 402 queries
    \item train: every other node, 2,191 on Cora\_ML
    \item hard subset: 64 val nodes where the depth-1 BFS vote is
      wrong
  \end{itemize}

  \vfill

  Splits and hard subset are hash-pinned at creation time.
\end{frame}

\begin{frame}{Baselines and BP}
  \begin{itemize}
    \item \textbf{BFS depth 1}
    \begin{itemize}
      \item immediate outgoing citation neighborhood
      \item cheapest baseline
    \end{itemize}
    \item \textbf{BFS depth 2/3}
    \begin{itemize}
      \item broader local context in validation sweep
      \item larger neighborhoods
    \end{itemize}
    \item \textbf{BP}
    \begin{itemize}
      \item branch-and-price for directed density
      \item validation sweep over $k$ and $\kappa$
    \end{itemize}
    \item \textbf{Avgdeg}
    \begin{itemize}
      \item exact average-degree baseline on local BFS graph
    \end{itemize}
  \end{itemize}
\end{frame}

\begin{frame}{Exp 2: scope}
  \begin{itemize}
    \item intrinsic perspective: label-agnostic geometry of the
      returned community
    \item evaluated on val + test combined, so every queried node
      contributes
  \end{itemize}

  \vfill

  Locked grid:
  \begin{itemize}
    \item BFS depth $\in \{1, 2\}$
    \item BP $k \in \{3, 4, 5\}$, $\kappa \in \{0, 1, 2\}$
    \item AvgDeg single cell
    \item five Gurobi seeds for BP $(42..46)$; deterministic methods
      once
    \item no time, node, gap, or Dinkelbach limit
  \end{itemize}
\end{frame}

\begin{frame}{Exp 2: cluster quality on val + test}
  Median over the val + test pool per (method, $k$, $\kappa$).

  \vfill

  Reported metrics:
  \begin{itemize}
    \item community size
    \item algebraic connectivity $\lambda_2$
    \item edge density
    \item normalised cut
    \item mixing parameter
    \item within-class internal edge ratio
    \item true-class vote share
    \item train-label entropy
  \end{itemize}

  \vfill

  \textbf{Placeholder.} Numbers pending pipeline rerun.
\end{frame}

\begin{frame}{Exp 2: cost on val + test}
  Median over the val + test pool per (method, $k$, $\kappa$).

  \vfill

  Reported:
  \begin{itemize}
    \item wall time
    \item oracle queries
    \item BP branch-and-bound nodes
    \item BP LP solves
    \item BP columns added
    \item BP cuts added
    \item LP solve time
    \item pricing time
    \item returned community size
  \end{itemize}

  \vfill

  \textbf{Placeholder.} Numbers pending pipeline rerun.
\end{frame}

\begin{frame}{Exp 3: scope and prediction rule}
  \begin{itemize}
    \item extrinsic perspective
    \item tune on val, evaluate on test and on the BFS-hard val
      subset
  \end{itemize}

  \vfill

  For each val or test query $q$:
  \begin{itemize}
    \item retrieve a community around $q$ from the chosen method
    \item predict $q$'s label by a uniform majority vote over the
      training-labelled members of that community
    \item ties broken by ascending label so the result is
      reproducible
    \item fallback when the community has no training neighbour: walk
      outward by BFS, forbidding val and test nodes as stepping
      stones
    \item last resort if no training label is reachable: the global
      majority of the training pool
  \end{itemize}
\end{frame}

\begin{frame}{Exp 3: tuning on val}
  Pick the best params per method by macro F1 on val:
  \begin{itemize}
    \item AvgDeg: single cell
    \item BFS: best of depth $\in \{1, 2\}$
    \item BP: best of $(k, \kappa)$ on the locked grid, seed 42
  \end{itemize}

  \vfill

  Reported:
  \begin{itemize}
    \item chosen params per method
    \item val macro precision, recall, F1
  \end{itemize}

  \vfill

  \textbf{Placeholder.} Numbers pending pipeline rerun.
\end{frame}

\begin{frame}{Exp 3: evaluation on test}
  Apply the best params per method to the test split.
  \begin{itemize}
    \item 402 test queries on Cora-ML
    \item mean over five Gurobi seeds (42..46)
    \item bootstrap $95\%$ CI from $B = 500$ resamples
    \item resample indices shared across methods so the CI is paired
      by query
    \item deterministic methods (AvgDeg, BFS): single-seed paired CI
    \item BP: stratified-by-seed bootstrap
  \end{itemize}

  \vfill

  Reported: macro precision, recall, F1.

  \vfill

  \textbf{Placeholder.} Numbers pending pipeline rerun.
\end{frame}

\begin{frame}{Exp 3: hard subset motivation}
  The full test split mixes two regimes.

  \vfill

  \begin{itemize}
    \item easy regime: the immediate citations already carry the
      right label, so depth-1 BFS is hard to beat
    \item hard regime: the immediate citations carry the wrong label,
      so the retrieval has to look further
  \end{itemize}

  \vfill

  BFS-hard val subset:
  \begin{itemize}
    \item val nodes where the depth-1 BFS vote disagrees with the
      true label
    \item 64 nodes on Cora-ML, fixed at split-creation time
  \end{itemize}
\end{frame}

\begin{frame}{Exp 3: evaluation on the hard subset}
  Apply the best params per method to the BFS-hard val nodes.
  \begin{itemize}
    \item 64 val queries on Cora-ML
    \item mean over five seeds (42..46)
    \item bootstrap $95\%$ CI ($B = 500$)
    \item BFS depth 1 scores 0 by construction
    \item Wilson interval replaces the bootstrap CI when every
      replicate gives the same value (typical for BFS here)
  \end{itemize}

  \vfill

  \textbf{Placeholder.} Numbers pending pipeline rerun.
\end{frame}

\section{OpenAlex frontend}

\begin{frame}{From simulation to live citation graph}
  \begin{itemize}
    \item the experiments so far ran on a local CSV of Cora-ML
    \item the same solver speaks to OpenAlex, an open citation
      database
    \item one query returns a paper's outgoing references and (capped)
      incoming citations
    \item every node materialised by the solver is fetched live
    \item lazy oracle means the user pays only for the papers BP
      actually wants
  \end{itemize}
\end{frame}

\begin{frame}{Web frontend}
  \begin{itemize}
    \item enter a paper as the query
    \item watch the active set and frontier evolve as the solver runs
    \item dense community + boundary citations rendered as a graph
    \item per-paper metadata pulled live from OpenAlex
    \item useful as a reading-list builder for a single query paper
  \end{itemize}
\end{frame}

\section{Conclusion}

\begin{frame}{Main interpretation}
  \textbf{Placeholder.} Numbers pending pipeline rerun. The
  expected story:

  \vfill

  \begin{itemize}
    \item BP returns a denser, better-mixed community at a size
      comparable to depth-1 BFS
    \item depth-1 BFS wins macro F1 on the full test split because
      most test papers cite same-class training papers
    \item on the BFS-hard subset the geometry picture stays the
      same; classification ranks shift because BFS is zero by
      construction there
  \end{itemize}

  \vfill

  Claim:
  \begin{itemize}
    \item BP is a dense-community retriever
    \item its classification F1 follows from the geometry, not the
      other way around
  \end{itemize}
\end{frame}

\begin{frame}{Limitations}
  \begin{itemize}
    \item Reported numbers are Cora\_ML only. CiteSeer, DBLP, and Cora runs
      are in progress.
    \item The temporal split makes one-hop BFS unusually strong: test nodes
      are pure sources whose direct citations are in the train set.
    \item The hard subset is hash-pinned but defined on validation; it is a
      diagnostic, not a held-out test.
    \item BP relies on Gurobi.
    \item Exact global optimality is over the discovered graph region; the
      oracle interface limits what BP can reason about.
    \item OpenAlex mode depends on API coverage and latency.
  \end{itemize}
\end{frame}

\begin{frame}{Next steps}
  \begin{itemize}
    \item Multi-dataset classification experiments
    \item Synthetic planted-community benchmarks
    \item Ablate:
    \begin{itemize}
      \item $\kappa$ cuts
      \item triangle cuts
      \item branching rule
      \item pruning
      \item batch pricing
    \end{itemize}
    \item Add latency-aware OpenAlex experiments
    \item Explore hybrid objectives:
    \[
      \text{density} + \text{label purity} + \text{semantic similarity}
    \]
  \end{itemize}
\end{frame}

\begin{frame}{Takeaways}
  \begin{itemize}
    \item Query-anchored dense community search is a different task from BFS classification
    \item Dinkelbach handles the fractional density objective
    \item Branch-and-price handles binary search over a lazy active graph
    \item Pricing decides which frontier papers to fetch
    \item Connectivity can be enforced lazily by min-cut cuts
    \item Results show a two-regime story:
    \begin{itemize}
      \item BFS wins easy local-label cases
      \item BP is useful for compact constrained retrieval
    \end{itemize}
  \end{itemize}
\end{frame}

\begin{frame}{}
  \centering
  \Large
  \emph{Thank you!}
\end{frame}

\end{document}
